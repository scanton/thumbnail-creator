# Vercel Blob Storage token
# Create a Blob Store in your Vercel project dashboard, then copy the token here.
# On Vercel, set this in Project Settings → Environment Variables.
# Required for thumbnail persistence — app will throw a clear error on startup if missing.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_your_token_here
